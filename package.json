{
  "name": "hidrajs-winston-logger",
  "version": "1.0.1",
  "description": "@hidrajs/winston-logger winston custom logger package",
  "main": "dist/logger.js",
  "types": "dist/logger.d.ts",
  "keywords": [
    "logger"
  ],
  "author": "hdrajs",
  "license": "ISC",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "release": "pnpm build && pnpm publish --access public --no-git-checks",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hidrajs/winston-logger.git"
  },
  "dependencies": {
    "util": "^0.12.5",
    "winston": "^3.17.0",
    "winston-daily-rotate-file": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
